<{{ original_url }}>;rel="original",
 <{{ timemap_url }}>
   ; rel="self";type="application/link-format"
   ; from="{{ minimum_datetime|date:"r" }}"
   ; until="{{ maximum_datetime|date:"r" }}",{% for item in items %}
 <{{ item.link }}>
   ; rel="{% if item.first %}first {% endif %}{% if item.last %}last {% endif %}memento"; datetime="{{ item.datetime|date:"r" }}"{% if not forloop.last %},{% endif %}{% endfor %}